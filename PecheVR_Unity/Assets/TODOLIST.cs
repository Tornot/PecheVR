using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChosesAFaire : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {/*
        -Apporter toutes les boîtes, les drivers moteur sont chez moi!
        -Apporter mon pc, le code arduino est dessus.

        -Les deux fichiers qui gèrent la connexion arduino : "ArduinoConnector.cs" et "SerialPortManager.cs".
        -Avoir le moteur qui tourne quand le poisson tire
        -Limiter le déplacement pour que le poisson et le flotteur restent dans l'alignement du moteur.

         
         
         */

    }

    // Update is called once per frame
    void Update()
    {

    }
}
